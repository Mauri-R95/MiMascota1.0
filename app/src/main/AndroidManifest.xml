<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mauri_r95.mimascota10">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:noHistory="true"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MascotaActivity"
            android:label="@string/mascota"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".RegistrarActivity"
            android:label="@string/registrar" />
        <activity
            android:name=".PublicarAnuncioActivity"
            android:label="@string/publicar_anuncio" />
        <activity
            android:name=".CategoriaActivity"
            android:label="@string/categoria" />
        <activity
            android:name=".TipoMascotaActivity"
            android:label="@string/tipo_mascota" />
        <activity
            android:name=".TamanoActivity"
            android:label="@string/tamano" />
        <activity
            android:name=".ComunaActivity"
            android:label="@string/comuna" />
        <activity
            android:name=".ProvinciaActivity"
            android:label="@string/provincia" />
        <activity
            android:name=".RazaActivity"
            android:label="@string/raza" />
        <activity
            android:name=".RegionActivity"
            android:label="@string/region" />
        <activity
            android:name=".MiCuentaActivity"
            android:label="@string/mi_cuenta" />
        <activity android:name=".BuscarActivity" />
        <activity android:name=".EditarCuentaActivity" />
        <activity android:name=".CambiarPassActivity" />
        <activity android:name=".MisActivity"></activity>
    </application>

</manifest>